<UserControl x:Class="ModelModule.Views.Navigation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ModelModule.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
           
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Right" Margin="10,10,10,10">
            <Button Content="Назад"  Height="40" Width="80" Command="{Binding PreviousPage}" Margin="0,0,15,0" FontFamily="Microsoft JhengHei Light" FontSize="20" />
            <Button Content="Далее"  Height="40" Width="80" Command="{Binding NextPage}" FontFamily="Microsoft JhengHei Light" FontSize="20" />
        </StackPanel>
    </Grid>
</UserControl>
