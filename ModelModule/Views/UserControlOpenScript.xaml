<UserControl x:Class="ModelModule.Views.UserControlOpenScript"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ModelModule.Views"
             mc:Ignorable="d" 
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DesignHeight="450" d:DesignWidth="800">

    <d:UserControl.Resources>
        <ResourceDictionary Source="pack://application:,,,/Prisma.Core;component/Resources/MainResource.xaml"/>
    </d:UserControl.Resources>
    <Border BorderBrush = "White" BorderThickness="2" >
        <Grid >
            <Button Content="Обзор" Width="100" Height="30"  HorizontalAlignment="Left" VerticalAlignment="Top" Command="{Binding _openScriptPy}" Style="{StaticResource ButtonStyle}"/>
            <TextBlock Text="{Binding FilePath}"  Margin="20,54,26,28" Style="{StaticResource TextBlockStyle}" />
        </Grid>
    </Border>
</UserControl>
